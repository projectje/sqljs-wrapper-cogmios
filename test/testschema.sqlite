CREATE TABLE IF NOT EXISTS itemUrl (id INTEGER PRIMARY KEY AUTOINCREMENT, url TEXT);
CREATE TABLE IF NOT EXISTS itemProperty (id INTEGER PRIMARY KEY AUTOINCREMENT,itemUrlId INTEGER,itemkey TEXT,itemvalue TEXT,CONSTRAINT itemUrl_fk_itemProperty FOREIGN KEY (itemUrlId) REFERENCES itemUrl(id) ON UPDATE CASCADE ON DELETE CASCADE);